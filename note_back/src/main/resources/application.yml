spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/note_note?characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: txd123456
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: 'update'
      use-new-id-generator-mappings: true
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
server:
  port: 8319